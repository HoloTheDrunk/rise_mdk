return {
  StateManager = require("mdk.utils.StateManager"),
  Transition = require("mdk.utils.Transition"),
  TransitionManager = require("mdk.utils.TransitionManager"),
  oop = require("mdk.utils.oop"),
  -- Submodules
  imgui = require("mdk.utils.imgui._mod"),
}
